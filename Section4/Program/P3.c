#include <stdio.h>

int main(void)
{
    int hun, ten, one, result;
    printf("This is a try. Enter number: ");
    scanf("%1d%1d%1d", &hun, &ten, &one);
    printf("The reversal is: %d%d%d\n", one, ten, hun);
    return 0;
}